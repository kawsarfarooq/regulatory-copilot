# Five-minute demonstration

1. Show `docker compose ps` with the Outline stack healthy.
2. Open Outline and show the `Regulatory Intelligence` collection and three FDA sources.
3. Show `tmux ls` and the vLLM health response on the GPU server.
4. Show the workstation SSH tunnel health response.
5. Open Continue Agent mode and select `Qwen Regulatory Coder`.
6. Ask the SRS grounded prompt from the root README.
7. Point out the single `outline_research` tool invocation.
8. Show the answer citing FDA Device Software Premarket Guidance (2023), PDF page 22.
9. Explain that the MCP adapter is read-only and that secrets are absent from the repository.
10. Finish with the architecture diagram and production-hardening limitations.

Avoid displaying API keys, generated administrator credentials, `.env` files, or the Outline token during the recording.
